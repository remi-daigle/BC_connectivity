if (!require("pacman")) install.packages("pacman")
pacman::p_load(rfigshare,
               rmarkdown,
               readr,
               fields,
               dplyr,
               magrittr,
               rgeos,
               rgdal,
               tidyr,
               ggplot2,
               gridExtra,
               raster,
               mapdata,
               marmap,
               maptools
               )
